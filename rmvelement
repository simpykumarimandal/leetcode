class Solution {
    public int removeElement(int[] nums, int val) {
        int write_idx = 0;

        for(int i =0; i< nums.length;i++){
            if(nums[i]!= val){
                nums[write_idx] =nums[i];
                write_idx++;
            }
        }
        return write_idx;
        
    }
}
